import * as React from 'react';
import ScopedCssBaseline from '@momo/material/ScopedCssBaseline';

<ScopedCssBaseline
  sx={(theme) => ({
    backgroundColor: theme.palette.background.paper,
  })}
/>;
